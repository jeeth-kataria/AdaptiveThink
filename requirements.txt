# AdaptiveThink Requirements
# Install with: pip install -r requirements.txt

# Core ML libraries
torch>=2.1.0
transformers==4.46.3
accelerate==1.1.1
peft==0.13.2
bitsandbytes==0.44.1
trl==0.12.1

# Data & utilities
datasets==3.1.0
scipy==1.14.1
numpy==1.26.4
sentencepiece==0.2.0

# Experiment tracking
wandb==0.18.7
huggingface-hub==0.26.5

# API clients
openai==1.54.0
python-dotenv==1.0.1

# Optional: Unsloth (memory-efficient training)
# Install from git after base requirements:
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"

# Optional: flash-attn (faster attention, requires CUDA 11.8+)
# Install with: pip install flash-attn --no-build-isolation

# Optional: vLLM (fast generation, requires >=22GB VRAM)
# Install with: pip install vllm==0.6.4.post1
